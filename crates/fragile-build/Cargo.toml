[package]
name = "fragile-build"
version = "0.1.0"
edition = "2021"
description = "Build system integration for Fragile compiler"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
thiserror = "2.0"

[dev-dependencies]
tempfile = "3.4"
